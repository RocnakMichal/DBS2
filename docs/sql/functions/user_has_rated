CREATE OR REPLACE FUNCTION user_has_rated(user_id INT, movie_id INT)
RETURNS BOOLEAN AS $$
BEGIN
  RETURN EXISTS (
    SELECT 1
    FROM rating
    WHERE user_id = user_has_rated.user_id
      AND movie_id = user_has_rated.movie_id
  );
END;
$$ LANGUAGE plpgsql;